version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    open-pull-requests-limit: 5
    labels:
      - "type:chore"
      - "area:infra"

  # Python updates remain manual: exact uv, one direct tool per reviewed PR,
  # seven-day cutoff, both platform wheel/license checks (ADR-0002).
  # Never rewrite frozen experiments/toolchain-20260906 research evidence.
